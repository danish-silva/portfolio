# dependencies
node_modules/

# build output
dist/
.astro/

# Cloudflare
.wrangler/

# env
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# agent skills installed locally with `npx skills add` (not published)
.agents/
.claude/
skills-lock.json

# Untouched originals of media that was re-encoded or cropped for the site.
# Kept out of the repo because they are large and the site never serves them.
media-sources/
